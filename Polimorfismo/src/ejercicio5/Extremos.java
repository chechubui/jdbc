package ejercicio5;

public interface Extremos {
	
	//no hay que poner public o private porque estas ya son por defecto publicas
	
	int min(int [] a);
	int max(int [] a);
	double min(double [] a);
	double max(double [] a);
	
}
